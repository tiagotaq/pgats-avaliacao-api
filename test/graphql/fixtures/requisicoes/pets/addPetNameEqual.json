{
    "query": "mutation Mutation($name: String!, $type: String!) { addPet(name: $name, type: $type) { id name type userId } }",
    "variables": {
        "name": "Luke",
        "type": "Nome Igual"
    }
}